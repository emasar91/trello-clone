import { SxProps } from '@mui/material'
import { Theme } from '@mui/system'

export const EmailFieldContainerStyle: SxProps<Theme> = {
	position: 'relative',
	display: 'flex',
	flexDirection: 'column',
	gap: '8px',
	marginBottom: '12px',
	width: '100%',
}
